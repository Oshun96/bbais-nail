/** Fallback only — the API's `derived.day_labels` is the source of truth. */
export const DAY_LABELS_FALLBACK = {
  mon: 'Monday', tue: 'Tuesday', wed: 'Wednesday', thu: 'Thursday',
  fri: 'Friday', sat: 'Saturday', sun: 'Sunday',
}
